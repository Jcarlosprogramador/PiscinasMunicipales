<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:padding="4dp"
    android:background="@drawable/piscinas3"
    >

    <TextView
        android:id="@+id/tv_prin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tv_titulo"
        android:textStyle="bold"
        android:textSize="28dp"
        android:textAlignment="center"
      />
    <TextView
        android:id="@+id/tvEstilos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/tv_municipios"
        android:layout_below="@id/tv_prin"
        android:textStyle="bold"
        android:textSize="18sp"
        android:paddingLeft="20sp"
        />
    <TextView
        android:id="@+id/tvMuni"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/tv_todos_municipios"
        android:layout_below="@id/tvEstilos"
        android:textSize="16dp"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        />
    <EditText
        android:id="@+id/etMunicios"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvMuni"
        android:textSize="16dp"
        android:textColor="@android:color/white"
        android:textStyle="bold"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etMunicios"
        android:text="@string/btn_consulta"
        android:onClick="Consulta"/>

</RelativeLayout>